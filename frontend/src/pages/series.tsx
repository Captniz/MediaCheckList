import { useEffect, useState } from "react";
import Navbar from "../components/navbar";

const Series = () => {
  return (
    <div className="home">
        <Navbar/>
    </div>
  );
};

export default Series;