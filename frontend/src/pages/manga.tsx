import { useEffect, useState } from "react";
import Navbar from "../components/navbar";

const Manga = () => {
  return (
    <div className="home">
        <Navbar/>
    </div>
  );
};

export default Manga;